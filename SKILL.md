---
name: skill-fusion
disable-model-invocation: true
author: 本地自研
description: |
  技能融合流水线：把货架候选（CANDIDATES.md）融进已有技能，并以验证门禁证明「增强了且没弄坏」。
  触发：融进去、融合、升级技能、升级这个技能、把XX融进、货架取用、从货架装、这个候选融了吧、验证升级、融合后验证。
  不触发：从无到有写新技能（→ cangjie）；评分驱动优化已有技能（→ darwin-skill）；装第三方技能入库（→ AGENTS.md 规则 6/7）；发现候选（→ search / 养料侦察）。
---

# skill-fusion · 技能融合流水线

> **核心心智**：融合 ≠ 复制粘贴。每一步都有验证，最终必须用「正例 + 负例 + 回归 + 固化」四件套证明增强，否则不算完成。今天不验证，明天就生产 bug。

## 何时用

用户说「融进去 / 升级技能 / 把 XX 融进 / 货架取用」时。前置：候选已在 `~/.dsh/skills/CANDIDATES.md` 货架；没有先走 search 侦察登记。

## 流程（8 步，顺序执行）

### 1. 取候选 + 查重

- 读 CANDIDATES.md 对应行，确认候选、目标技能、货架注记的拦路点。
- **查「已吸收」**：`git -C ~/.dsh/skills log --oneline -10 -- <目标技能>` 看是否已融过（货架状态可能过期）。已融 → 只更新货架状态，不重复融合。

### 2. 装前审查（上游源码）

- clone/tarball 拉上游（git 的 LibreSSL 失败时用 curl tarball 或 GitHub API）。
- 仓库 404/改名 → GitHub 搜索找实际源，修正货架链接再继续。
- 拦路点未变（依赖/权重/架构级）→ 不融，货架留注。

### 3. 差距分析（派子代理）

派 `subagent_minimax`（长文）产出差距表，prompt 必须包含：上游文件路径、本地目标技能全部相关文件、要求输出 A 未吸收（带 file:line）/ B 已吸收确认 / C 不融理由 / D 融合建议（目标文件+一句话改法）。不重复派正在跑的调研。

### 4. 🔴 融合范围确认（检查点）

向用户报「融什么 / 不融什么（理由）」，**用户确认后才动手**。货架拦路点标注的项默认不融，除非用户明说。

### 5. 实施融合

- 脚本改动只加不改（新增检测器/参数，不动既有类别行为）；文档改动挂最小挂载点（方法论清单/触发词），不动脚本硬校验的字段结构。
- 写入 `~/.dsh/skills` 被沙箱挡 → 提权一次；权限被拒即停，不绕路。

### 6. 验证门禁（🔴 硬性，跳过=融合失败）

四件套全过才算「增强」：

1. **正例**：每个新检测/新功能用最小输入验证命中（构造最小触发样本）。
2. **负例/边界**：正常文本零误报；边界至少验：编码（BOM/GBK）、台词豁免（引号内不误报）、参数组合（whitelist+extra 同用）、单处不报阈值。
3. **回归**：`bash {SKILL_DIR}/scripts/verify.sh <目标技能目录绝对路径> <改动文件...>`（lint + 技能自带回归 + 改动文件语法）。
4. **固化**：正例/负例/边界测试写进目标技能 `tests/` 并接入其 `run_regression.sh`（没有就建）；「临时测试没固化」= 验证不合格。

发现 bug → 按 systematic-debugging 流程修，修复后重跑四件套。

### 7. 登记 + commit

- CANDIDATES.md：状态改「已吸收（日期）+ 融了什么」，修正过期链接；不融项保留并更新理由。
- SKILLS.md：目标技能条目追加本次融合摘要。
- `cd ~/.dsh/skills && git add -A && git commit -m "feat(<技能>): 融合 <候选> — <一句话>"`；bug 修复单独 commit `fix(...)`。

### 8. 汇报

只报：融了什么 / 验证证据（正例命中、负例零误报、回归全绿）/ 没融什么（理由）。不报无证据的「增强了」。

## 失败模式

| 情形 | 处置 |
|------|------|
| 上游仓库 404/改名 | GitHub 搜索实际源，货架链接一并修正 |
| git clone SSL 失败 | curl tarball（分支名先查 API default_branch）或 GitHub API 取文件 |
| 验证不过 | 回滚该改动，重做；同一位置两次不过 → 🔴 停，问用户 |
| 目标技能无 tests/ | 跑 verify.sh 会 fail；按第 6.4 条建测试后重跑 |
| commit 失败（git 未配置） | 先补 git config 再提交；仍失败 → 报告并保留改动 |

## 不要做

- ❌ 不跳过验证门禁任何一件（正例/负例/回归/固化）
- ❌ 不把「已吸收但货架未更新」的过期候选再融一遍
- ❌ 不整库照搬上游——只融差距表里的高性价比项，不融项写理由
- ❌ 不动目标技能脚本硬校验的字段结构（如 outline.md 五块、账本协议）
- ❌ 不用临时 /tmp 测试代替固化测试
- ❌ 不重复子代理正在跑的调研
- ❌ 不把本技能用于「写新技能」（→ cangjie）与「评分优化」（→ darwin-skill）

## 分工注记（防重复安装）

- **cangjie** = 从无到有造新技能；**darwin-skill** = 评分驱动的自主优化（rubric 爬山）；**skill-fusion** = 货架驱动的候选融合 + 验证门禁。三者分工：造 / 优 / 融，不互相替代。
- 装第三方（不动源码直接入库）是 AGENTS.md 规则 6/7 的公共动作，不走本技能。

## 诚实边界

- 本技能管「融合 + 验证」流程；候选怎么发现（search/养料侦察）不归本技能。
- 融合内容的专业质量由差距分析子代理 + 用户确认把关；本技能只保证流程完整。
- 深度质量评分交给 darwin-skill；本技能的「增强判定」= 验证门禁过 + 证据汇报。
